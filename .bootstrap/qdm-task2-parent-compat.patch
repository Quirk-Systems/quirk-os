--- a/scripts/validate_daily_move_fixtures.py
+++ b/scripts/validate_daily_move_fixtures.py
@@ -2,11 +2,23 @@
     markers: set[str] = set()
     allowed = {
         ".github/workflows/daily-move-fixtures.yml",
+        ".github/workflows/daily-move-io-conformance.yml",
+        "docs/superpowers/plans/2026-08-21-quirk-daily-move-io.md",
+        "docs/superpowers/specs/2026-08-21-quirk-daily-move-io-design.md",
         "scripts/validate_daily_move_fixtures.py",
+        "scripts/validate_daily_move_io.py",
         "tests/test_daily_move_fixtures.py",
+        "tests/test_daily_move_io.py",
+        "tests/test_daily_move_io_workflow.py",
+        "tests/test_daily_move_task1_compatibility.py",
         "evals/daily-move/README.md",
         "evals/daily-move/fixtures.json",
         "evals/daily-move/conformance-results.json",
+        "evals/daily-move/io-cases/invalid-cases.json",
+        "evals/daily-move/io-cases/valid-input.json",
+        "evals/daily-move/io-cases/valid-output.json",
+        "schemas/daily-move-input.schema.json",
+        "schemas/daily-move-output.schema.json",
         *(f"evals/daily-move/cases/{case_id}.json" for case_id in EXPECTED_IDS),
     }
 
--- a/.github/workflows/daily-move-io-conformance.yml
+++ b/.github/workflows/daily-move-io-conformance.yml
@@ -10,6 +10,7 @@
       - 'scripts/validate_daily_move_fixtures.py'
       - 'tests/test_daily_move_io.py'
       - 'tests/test_daily_move_io_workflow.py'
+      - 'tests/test_daily_move_task1_compatibility.py'
       - 'tests/test_daily_move_fixtures.py'
   push:
     branches-ignore: [main]
@@ -21,6 +22,7 @@
       - 'scripts/validate_daily_move_fixtures.py'
       - 'tests/test_daily_move_io.py'
       - 'tests/test_daily_move_io_workflow.py'
+      - 'tests/test_daily_move_task1_compatibility.py'
       - 'tests/test_daily_move_fixtures.py'
   workflow_dispatch:
 
@@ -36,10 +38,15 @@
     name: daily-move-io-conformance
     runs-on: ubuntu-24.04
     timeout-minutes: 10
+    env:
+      PYTHONDONTWRITEBYTECODE: '1'
 
     steps:
       - name: Checkout
         uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
+        with:
+          fetch-depth: 0
+          persist-credentials: false
 
       - name: Set up Python
         uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
@@ -51,21 +58,24 @@
       - name: Install evaluation dependencies
         run: python -m pip install --disable-pip-version-check -r requirements-evals.txt
 
+      - name: Validate Task 1 fixture corpus
+        run: python scripts/validate_daily_move_fixtures.py --require-pass
+
+      - name: Run Task 1 tests
+        run: python -m unittest tests.test_daily_move_fixtures -v
+
+      - name: Run Task 1 compatibility tests
+        run: python -m unittest tests.test_daily_move_task1_compatibility -v
+
       - name: Initialize Daily Move IO evidence
+        if: always()
         run: |
           printf '%s\n' '{"schema_version":"daily-move.io-conformance.v1","valid_pair":false,"finding_codes":["VALIDATION_NOT_RUN"],"external_writes":0,"authority_ceiling":"propose"}' > daily-move-io-conformance.json
 
-      - name: Validate Task 1 fixture corpus
-        run: python scripts/validate_daily_move_fixtures.py --require-pass
-
       - name: Validate Task 2 IO contracts
         if: always()
         run: python scripts/validate_daily_move_io.py --require-pass --report daily-move-io-conformance.json
 
-      - name: Run Task 1 tests
-        if: always()
-        run: python -m unittest tests.test_daily_move_fixtures -v
-
       - name: Run Task 2 tests
         if: always()
         run: python -m unittest tests.test_daily_move_io -v
--- /dev/null
+++ b/tests/test_daily_move_task1_compatibility.py
@@ -0,0 +1,77 @@
+from __future__ import annotations
+
+import ast
+import tempfile
+import unittest
+from pathlib import Path
+
+from scripts.validate_daily_move_fixtures import _implementation_markers, validate_repo
+
+ROOT = Path(__file__).resolve().parents[1]
+CONTRACT_PATHS = {
+    ".github/workflows/daily-move-io-conformance.yml",
+    "docs/superpowers/plans/2026-08-21-quirk-daily-move-io.md",
+    "docs/superpowers/specs/2026-08-21-quirk-daily-move-io-design.md",
+    "evals/daily-move/io-cases/invalid-cases.json",
+    "evals/daily-move/io-cases/valid-input.json",
+    "evals/daily-move/io-cases/valid-output.json",
+    "schemas/daily-move-input.schema.json",
+    "schemas/daily-move-output.schema.json",
+    "scripts/validate_daily_move_io.py",
+    "tests/test_daily_move_io.py",
+    "tests/test_daily_move_io_workflow.py",
+    "tests/test_daily_move_task1_compatibility.py",
+}
+
+
+class DailyMoveTask1CompatibilityTests(unittest.TestCase):
+    def test_exact_contract_surfaces_do_not_trigger_runtime_containment(self) -> None:
+        markers = set(_implementation_markers(ROOT))
+        self.assertTrue(CONTRACT_PATHS.isdisjoint(markers), sorted(CONTRACT_PATHS & markers))
+        codes = {finding["code"] for finding in validate_repo(ROOT)["findings"]}
+        self.assertNotIn("RUNTIME_CONTAINMENT_REQUIRED", codes)
+
+    def test_nearby_alias_and_real_runtime_namespace_still_trigger(self) -> None:
+        with tempfile.TemporaryDirectory() as temp_dir:
+            root = Path(temp_dir)
+            alias = root / "schemas/daily-move-io.schema.json"
+            runtime = root / "scripts/daily_move/policy.py"
+            alias.parent.mkdir(parents=True)
+            runtime.parent.mkdir(parents=True)
+            alias.write_text('{"title":"Daily Move IO"}\n', encoding="utf-8")
+            runtime.write_text("def evaluate_daily_move_case(scenario, adapters):\n    return {}\n", encoding="utf-8")
+            markers = set(_implementation_markers(root))
+            self.assertIn("schemas/daily-move-io.schema.json", markers)
+            self.assertIn("scripts/daily_move/policy.py", markers)
+
+    def test_contract_validator_has_no_runtime_or_external_effect_surface(self) -> None:
+        path = ROOT / "scripts/validate_daily_move_io.py"
+        tree = ast.parse(path.read_text(encoding="utf-8"), filename=str(path))
+        imports = {
+            name
+            for node in ast.walk(tree)
+            for name in (
+                [alias.name.split(".", 1)[0] for alias in node.names]
+                if isinstance(node, ast.Import)
+                else [node.module.split(".", 1)[0]]
+                if isinstance(node, ast.ImportFrom) and node.module
+                else []
+            )
+        }
+        allowed = {"__future__", "argparse", "copy", "datetime", "hashlib", "json", "jsonschema", "pathlib", "typing", "zoneinfo"}
+        self.assertEqual(set(), imports - allowed)
+        functions = {node.name for node in ast.walk(tree) if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef))}
+        self.assertTrue({"evaluate_daily_move_case", "generate_daily_move", "execute_daily_move", "publish_daily_move"}.isdisjoint(functions))
+
+    def test_io_workflow_proves_parent_before_creating_local_report(self) -> None:
+        workflow = (ROOT / ".github/workflows/daily-move-io-conformance.yml").read_text(encoding="utf-8")
+        self.assertIn("fetch-depth: 0", workflow)
+        self.assertIn("persist-credentials: false", workflow)
+        self.assertIn("PYTHONDONTWRITEBYTECODE", workflow)
+        self.assertLess(workflow.index("Validate Task 1 fixture corpus"), workflow.index("Initialize Daily Move IO evidence"))
+        self.assertLess(workflow.index("Run Task 1 tests"), workflow.index("Initialize Daily Move IO evidence"))
+        self.assertNotIn("contents: write", workflow)
+
+
+if __name__ == "__main__":
+    unittest.main()
