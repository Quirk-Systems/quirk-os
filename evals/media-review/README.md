# Media review regression catalog

Thirty synthetic test descriptors, paired with `tests/test_media_review.py`. Run the focused unittest suite from the candidate checkout. These are published regressions, not held-out data or a model benchmark. No Plugin Eval CLI run, numeric plugin score, model invocation, paid job, or Hugging Face publication is claimed.

A future dataset upload must specify an approved destination and data/license boundary, have repository-write permission, and verify post-upload content. Local packaging does not grant any of those permissions.
