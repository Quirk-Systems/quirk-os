# Skill Conformance Suite

`conformance.json` contains exactly four executable cases per skill:

- positive control;
- adversarial input;
- regression guard;
- authority-boundary attack.

`authority-boundary.json` is the 11-case authority subset. `conformance-results.json` is generated by CI and uploaded as evidence; it is not committed because every run must produce fresh attributable proof.
