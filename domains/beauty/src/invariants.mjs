export class InvariantError extends Error{constructor(code,message){super(message);this.name="InvariantError";this.code=code;}}
export function requireInvariant(condition,code,message){if(!condition)throw new InvariantError(code,message);}
export function assertChoice(choice,options){requireInvariant(choice?.sourceType==="explicit_human_choice","choice.not_explicit","Choice must be an explicit human choice.");requireInvariant(choice?.context?.realm==="beauty","choice.wrong_realm","Taste Engine v0.1 accepts only beauty contexts.");requireInvariant(typeof choice?.context?.purpose==="string"&&choice.context.purpose.length>0,"choice.missing_purpose","Choice requires a purpose partition.");requireInvariant(Array.isArray(choice?.presentedOptionIds)&&choice.presentedOptionIds.length>=2,"choice.insufficient_options","Choice requires at least two presented options.");const ids=new Set(options.map((option)=>option.id));for(const id of choice.presentedOptionIds)requireInvariant(ids.has(id),"choice.unknown_option",`Presented option ${id} is missing.`);if(choice.abstained){requireInvariant(choice.selectedOptionId===null,"choice.abstain_with_selection","An abstention cannot also select an option.");}else{requireInvariant(choice.selectedOptionId!==null,"choice.selection_required","A non-abstaining choice requires a selection.");requireInvariant(choice.presentedOptionIds.includes(choice.selectedOptionId),"choice.selection_out_of_scope","Selected option was not presented.");}}
export function assertExplicitOutcome(outcome){requireInvariant(outcome?.explicit===true,"outcome.not_explicit","Outcome must be explicitly reported by a human.");requireInvariant(outcome?.sourceType==="explicit_human_report","outcome.invalid_source","Clicks, purchases, silence, and engagement are not outcome reports.");requireInvariant(outcome?.purpose&&outcome?.actorId,"outcome.missing_scope","Outcome requires actor and purpose scope.");}
export function assertHumanDecision(decision){requireInvariant(decision?.humanConfirmed===true,"gate.not_human_confirmed","Graph update requires explicit human confirmation.");requireInvariant(["approve","revise","reject"].includes(decision?.decision),"gate.invalid_decision","Decision must approve, revise, or reject.");}
export function assertUnexpired(expiresAt,now=new Date()){requireInvariant(new Date(expiresAt).getTime()>now.getTime(),"gate.expired","Authority or proposal has expired.");}
